[package]
name = "localmt"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
localmt-bench = { path = "../localmt-bench" }
localmt-core = { path = "../localmt-core" }
localmt-engine = { path = "../localmt-engine" }
localmt-engine-llama = { path = "../localmt-engine-llama" }
localmt-engine-ort = { path = "../localmt-engine-ort" }
localmt-models = { path = "../localmt-models" }
localmt-pipeline = { path = "../localmt-pipeline" }
localmt-tokenizer = { path = "../localmt-tokenizer" }

[features]
default = []
hf-tokenizers = ["localmt-tokenizer/hf-tokenizers"]
llama-runtime = ["localmt-engine-llama/llama-runtime"]
ort-runtime = ["localmt-engine-ort/ort-runtime"]

[lints]
workspace = true
